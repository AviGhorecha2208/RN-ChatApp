enum Screens {
  StartupScreen = 'StartupScreen',
  Dashboard = 'Dashboard',
  RegisterUser = 'RegisterUser',
}

enum ToastType {
  success = 'success',
  error = 'error',
  info = 'info',
}

export { Screens, ToastType };
